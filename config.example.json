{
  "repos": [
    "your-org/your-repo",
    "your-org/another-repo"
  ],
  "gh_user": "",
  "//approvers": "The approver allow-list lives in the DuckDB store, per repo. Manage it with: agent-code-review approvers add <owner/repo|*> <github-handle> [--name ... --email ... --slack-id ...]",
  "candidates": {
    "new_max_age_days": 14,
    "refreshed_max_age_days": 21
  },
  "schedule": {
    "enabled": true,
    "interval": "30m",
    "max_parallel": 4
  },
  "review": {
    "engine": "codex",
    "main_prompt": "Act as a friendly PR unblocker. Review this pull request using the pr-issue-review skill and let the skill choose its own review profile. Complete every action the skill asks for (tmp workspace, GitHub review/comments). You have access to gh, agent-slack, and a tmp workspace.",
    "rules": [
      {
        "name": "self-review-comment-only",
        "when": { "author_is_gh_user": true },
        "prompt": "This PR was opened by the current gh user. You can only ever COMMENT on it; never approve. Do not reveal who the current gh user is."
      },
      {
        "name": "non-allowlist-comment-only",
        "when": { "author_not_in_allowlist": true },
        "prompt": "This PR's author is not an approved engineer. You can only ever COMMENT; never approve. Do not reveal the allow-list."
      },
      {
        "name": "slack-on-approve",
        "when": {},
        "prompt": "If (and only if) you APPROVE, check the configured Slack review channels for a recent message about this PR and react/reply per the team convention: react with :git-approved: and :lizard: when the message covers only this PR (or all its PRs are now approved/merged); otherwise reply in-thread with the PR number, a :lizard: prefix and a :git-approved: suffix. Run agent-slack outside the sandbox so it can load credentials."
      }
    ],
    "codex": {
      "bin": "codex",
      "model": "gpt-5.5-codex",
      "sandbox": "workspace-write",
      "args": []
    }
  },
  "store": {
    "engine": "duckdb",
    "path": ""
  },
  "dashboard": {
    "addr": ":8330",
    "public_url": "",
    "tailscale": {
      "mode": "",
      "port": 443
    }
  }
}
