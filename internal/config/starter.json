{
  "//repos_note": "The repos to discover candidate PRs in. Blank until you configure it: agent-code-review repos add <owner/name> [--unlisted <group>]. allowed_authors_only_repos is the pre-groups way to say \"only discover rostered authors here\"; it still works, and authors.unlisted supersedes it.",
  "repos": [],
  "allowed_authors_only_repos": [],
  "gh_user": "",
  "//authors_note": "Author groups. An author belongs to ONE group per repo, and the group is a complete review policy: what we may do with their PRs (ignore = never discovered, though a manual queue add still reviews; comment = reviewed but never approved; approve = approvable when the review warrants it), which engine reviews them, and what extra instruction the agent gets. WHO is in each group lives in the DuckDB store, per repo, because it churns: agent-code-review authors set <owner/repo|*> <github-handle> <group>. The groups themselves live here, next to the prompts they carry. approver / commenter / ignored exist without being declared; declaring one of those names replaces it. Resolution: the author's roster row (a repo-specific row beats a '*' row), else authors.unlisted for this repo, else authors.unlisted['*']. Then every matching override patches it field by field, where empty inherits and prompts accumulate. Run `agent-code-review authors who <handle> --repo <owner/name>` to see which layer decided what.",
  "authors": {
    "unlisted": {},
    "groups": {},
    "overrides": []
  },
  "//candidates_note": "rereview_cooldown holds a PR after one of our own reviews before it can be re-discovered; quiet_period holds a PR until it has gone untouched that long (no pushes or edits); steering_hold defers a PR while its author has the steering editor open, renewed while they type and capped so an editor left open cannot park it indefinitely. Holds are named and independent: a PR is eligible once every hold on it has expired, it keeps its queue position while held, and promotion or a manual add lifts them all. '0s' disables a hold.",
  "candidates": {
    "new_max_age_days": 14,
    "refreshed_max_age_days": 21,
    "rereview_cooldown": "90m",
    "quiet_period": "15m",
    "steering_hold": "5m",
    "error_backoff": "15m"
  },
  "//schedule_note": "Two independent loops, each with its own switch: discovery scrapes repos for candidates on its own poll (deterministic gh calls, no LLM); schedule consumes the queue (engine invocations, hence max_parallel). Reviews are dispatched one at a time as slots free, waiting dispatch_cooldown between hand-offs; interval is only the idle poll for when nothing is ready, and bounds how long a `queue add` from another process waits to be noticed.",
  "schedule": {
    "enabled": true,
    "interval": "30s",
    "max_parallel": 4,
    "dispatch_cooldown": "5s",
    "//usage_floor_note": "Hold a candidate when the engine that would review it has less than this % remaining in its 5h or weekly window; it runs when that window refills. Applied PER ENGINE, since a group can name its own: codex being out of headroom does not hold candidates bound for claude. A held candidate is never claimed or recorded, exactly like a cooldown hold. 0 disables a floor. Omit for the default of 10.",
    "usage_floor": {
      "5h_percent": 10,
      "weekly_percent": 10
    }
  },
  "discovery": {
    "enabled": true,
    "interval": "5m"
  },
  "review": {
    "//engine_note": "The DEFAULT agent CLI for reviews: \"codex\" or \"claude\". An author group can name its own engine/model/effort, which overrides this for that cohort's PRs, so one cycle can run both. Both report through the same verdict contract and write the same agent.log, so switching is a one-key change; only the matching engine block below applies, patched by whatever the resolved group asked for.",
    "engine": "codex",
    "//main_prompt_note": "The tool assumes only the gh CLI plus whichever engine CLI you selected. Your prompt may direct the agent to use anything else you have set up (skills such as pr-issue-review, extra CLIs such as agent-* tools); that is your prompt's business, and you grant any extra access via codex.args / claude.allowed_tools.",
    "main_prompt": "Act as a friendly PR unblocker. Review this pull request thoroughly: read the diff, the PR description, and any linked context via the gh CLI. Leave one GitHub review with inline comments that help the author move forward.",
    "//post_outcome_note": "on_approve / on_comment / on_reject run after the agent lands on that outcome (reject = requested changes). Put your workspace-specific conventions here (team channels, emoji rituals, notification tooling); the tool ships none of that.",
    "on_approve": "",
    "on_comment": "",
    "on_reject": "",
    "//rules_note": "Comment-only vs approval is a built-in, always-present directive (default: comment-only; approval only when the author's resolved group permits it and the PR isn't self-authored, which no group can override); you do not need a rule for it. A group's own prompt covers the unconditional case. Rules are for the conditional ones: they add EXTRA instructions when their condition matches (groups / authors / author_allowed / author_not_allowed / author_is_gh_user / candidate_type / repos). Give a rule an \"outcome\" (approve|comment|reject) to attach it under that post-outcome section instead of the prompt body. author_allowed now means \"the resolved policy permits approval\", so rules written before groups keep working. Manage rules with the 'prompts rules' command (agent-code-review prompts rules usage).",
    "rules": [],
    "//resume_prompt_note": "When a run ends without a final outcome (codex: an intermediate WORKING report; claude: no structured report at all), the driver resumes that session with this nudge instead of recording an ERROR. Empty uses a built-in default; the engine's max_resumes bounds the attempts.",
    "resume_prompt": "",
    "codex": {
      "bin": "codex",
      "//model_note": "Leave empty to use codex's own default model. To pin one, pick a valid id from `codex debug models`; an invalid id fails every review with a 400.",
      "model": "",
      "//effort_note": "Leave empty to use the selected model's default reasoning effort. To pin one, use an effort the selected model supports (today commonly low, medium, high, or xhigh).",
      "effort": "",
      "sandbox": "workspace-write",
      "//args_note": "workspace-write disables network by default, but the agent must reach GitHub via gh; this override re-enables network while keeping writes scoped to the per-PR workdir.",
      "args": [
        "-c",
        "sandbox_workspace_write.network_access=true"
      ],
      "//max_resumes_note": "How many times to resume a session that ended on a WORKING report before recording an ERROR. Unset means 2; 0 disables resuming.",
      "max_resumes": 2
    },
    "//claude_note": "Applies only when engine is \"claude\". Requires an authenticated Claude Code CLI (`claude auth login`); the tool never handles credentials. Reviews draw on that account's subscription, and schedule.usage_floor meters it the same way it meters codex.",
    "claude": {
      "bin": "claude",
      "//model_note": "Defaults to claude-opus-5 when empty. Pinned rather than inherited from your account's session default, so a review's depth and cost do not change under you when that default moves. Override with an alias (opus, sonnet, haiku, fable) or a full id. NOTE: the default 'auto' permission mode requires Opus 4.6+, Sonnet 4.6+, or Fable 5; haiku and older models are unsupported there and would fail every review, so pair them with a static permission_mode.",
      "model": "claude-opus-5",
      "//effort_note": "Defaults to medium when empty. Pinned rather than inherited from the session default, for the same reason as the model, plus one specific to this engine: a run reports no effort back, so an unpinned effort is also an unrecorded one and history could not correlate cost with effort. medium rather than the xhigh that general Opus 5 coding guidance suggests, because code review is the workload that guidance is least true of: this model holds precision and recall at lower effort. Raise this first if review quality slips. One of low, medium, high, xhigh, max.",
      "effort": "medium",
      "//permission_mode_note": "The analogue of codex.sandbox: what the agent may do without a prompt no headless run can answer. Default 'auto' routes each action through a classifier, so the agent can use whatever tools your prompt calls for without enumerating them here; it also strips tool RESULTS before judging, so a hostile PR diff cannot talk it into approving something. The static alternatives: acceptEdits (reads + file writes + common filesystem commands, everything else needs an allowed_tools entry), dontAsk (tighter still), bypassPermissions (no checks at all; only if you trust every prompt completely).",
      "permission_mode": "auto",
      "//allowed_tools_note": "Pre-approved tools, in permission-rule syntax. Leave EMPTY in auto mode: allow rules resolve ahead of the classifier, so listing something here exempts it from the vetting auto mode exists to do. In the static modes an empty list falls back to the minimum a review needs (gh, plus reads).",
      "allowed_tools": [],
      "//max_budget_usd_note": "Hard per-invocation ceiling (claude --max-budget-usd). On a subscription the figure is the notional API-rate valuation of the run, so this bounds a runaway review rather than literal spend. 0 means uncapped. Set it from your own data rather than guessing: every review records its cost, and the dashboard Metrics page shows the median and peak once a few have landed. A cap below your peak truncates real reviews, which is worse than no cap.",
      "max_budget_usd": 0,
      "//args_note": "Extra args appended to `claude -p`, for anything not covered above (--mcp-config, --add-dir, --agents).",
      "args": [],
      "//max_resumes_note": "How many times to resume a session that ended without a final report before recording an ERROR. Unset means 2; 0 disables resuming.",
      "max_resumes": 2
    }
  },
  "store": {
    "engine": "duckdb",
    "path": ""
  },
  "//scoring_note": "Gamifies PR authorship: every completed review earns the PR AUTHOR points. A score has two parts that answer two different questions. The SIZE reward asks how manageable the change was to review: it peaks at piece_lines points per line and at size_points for a single PR, and falls away on both sides, so the same solve in fewer lines is worth more and a stack of well-sized pull requests beats one enormous one. The REMOVAL reward asks whether the codebase got smaller, and simply pays for net removed lines. So +100/-100 scores 100 and +1000/-1000 scores 29; +100/-1000 scores 227 and +1000/-100 scores 47; 2000 lines shipped whole score 29 and the same lines as forty pieces of 50 score 2000, while 2000 one-line PRs score nothing at all, because the size reward is quadratic near zero. Scores are FROZEN when a review completes alongside a hash of these rules, so retuning anything here changes what FUTURE reviews earn and leaves existing points alone; `agent-code-review score recompute` re-applies new rules to old rows and `score refetch` re-measures from GitHub. See `agent-code-review score usage`.",
  "scoring": {
    "//mode_note": "Global switch. enabled = measure each reviewed PR and score it. leaderboard-only = STOP measuring (this removes the only ongoing network cost scoring adds, one GitHub call per review) while still showing the points already earned. disabled = no scoring, and the leaderboard is hidden in the dashboard. Can be narrowed per repo under scoring.repos.",
    "mode": "enabled",
    "//piece_lines_note": "Changed lines (added + removed) that earn the most points PER LINE. This is the size to aim for when splitting a large change into a stack of pull requests, and the whole scoring shape is built around it. Nothing here weighs a removed line differently from an added one: what a removal is worth is removal_points_per_100, kept separate so that 'a smaller solve is better' and 'a smaller codebase is better' cannot fight each other.",
    "piece_lines": 50,
    "//size_points_note": "The most a single PR can earn for its size alone, paid at the peak. It sets the scale of the whole leaderboard and nothing else; move it if the numbers feel too small or too large to care about.",
    "size_points": 100,
    "//size_falloff_note": "How sharply a PR stops being worth more as it grows. Must be above 2 (at 2 or below the curve has no peak and a bigger PR always earns more, which inverts the policy). It decides two things at once: the peak sits at piece_lines x (falloff-1) x 2 / (falloff-2), which is 200 changed lines at these defaults, and the premium for splitting a large change into a stack rather than shipping it whole. Higher falls harder and widens that premium; 3 makes 2000 lines worth 29 shipped whole against 2000 as forty pieces of 50.",
    "size_falloff": 3,
    "//removal_points_per_100_note": "Points for a hundred NET removed lines (deletions minus additions), paid on top of the size reward and independent of it. NET, so a pure move or rename earns nothing here: the review it cost is already paid by the size reward. Linear, so it is split-neutral: a thousand lines removed pays the same in one PR or in twenty. Set it to 0 to switch the removal reward off entirely.",
    "removal_points_per_100": 20,
    "//verdicts_note": "Multiplier per outcome. requested_changes is negative, so a rejected round actively costs points and a leaderboard can go below zero; set it to 0 if you would rather quality pressure came only from the decay. SKIPPED and ERROR never score: they are our machinery's outcomes, not the author's work.",
    "verdicts": {
      "approved": 1.0,
      "commented": 0.25,
      "requested_changes": -0.25
    },
    "//attempt_decay_note": "Applied as decay^(revision-1), so each further round of review on the same PR is worth less. Must be under 1: at exactly 1 nothing decays and comment-comment-approve would outscore getting it right first time. Counts REVISIONS, not reviews, so replying to the bot at the same commit costs nothing.",
    "attempt_decay": 0.4,
    "//use_gitattributes_note": "Read the repo OWN .gitattributes to leave linguist-generated and linguist-vendored files out of the size, honouring git own rules (every ancestor directory file applies, last match wins, and !attr undoes an earlier rule). This is the same declaration that collapses them in GitHub diff view, and it deliberately keeps the list of what counts as generated with each repo rather than here. A repo that has not marked its generated files will see them counted until it does. NOTE that exclusion settings are part of the RULES and are hashed with them, so changing one marks the rows scored under the old policy stale. They can be re-applied offline: each row stores its per-file measurement, so `score recompute --stale` re-decides what counts without asking GitHub again. `score refetch` is only for a row whose measurement is missing or describes a revision the review never saw.",
    "use_gitattributes": true,
    "//exclude_paths_note": "Extra globs excluded from the size, for repos that have not adopted linguist-generated. Gitignore-style: 'vendor/**', '*.pb.go', 'package-lock.json'.",
    "exclude_paths": [],
    "//repos_note": "Per-repo overrides, keyed 'owner/name'. Each entry patches the settings above field by field; buckets and exclude_paths REPLACE rather than append. Example: { \"owner/monorepo\": { \"base\": 50, \"exclude_paths\": [\"generated/**\"] } }",
    "repos": {}
  },
  "dashboard": {
    "addr": "127.0.0.1:8330",
    "public_url": "",
    "tailscale": {
      "mode": "",
      "port": 443
    }
  }
}
